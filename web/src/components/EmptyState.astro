---
interface Props {
  title: string;
  body?: string;
  class?: string;
}

const { title, body, class: className = '' } = Astro.props;
---

<div class={`state empty-state ${className}`}>
  <div class="ico">
    <slot name="icon">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="11" cy="11" r="7"></circle>
        <path d="m20 20-3.2-3.2"></path>
      </svg>
    </slot>
  </div>
  <h3>{title}</h3>
  {body && <p>{body}</p>}
  <slot name="action" />
</div>

<style>
  .empty-state {
    padding: 24px 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 8px;
  }
  .empty-state h3 {
    font-size: 16px;
  }
  .empty-state p {
    font-size: 13px;
    color: var(--text-dim);
    max-width: 380px;
  }
  .empty-state .ico {
    width: 42px;
    height: 42px;
    border-radius: 999px;
    background: var(--surface-2);
    display: grid;
    place-items: center;
    color: var(--text-dim);
  }
</style>
