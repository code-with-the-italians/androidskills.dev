dbs:
  - path: /data/androidskills.db
    replicas:
      - type: s3
        bucket: ${R2_BUCKET}
        path: ${R2_BACKUP_PATH:-androidskills/db}
        endpoint: ${R2_ENDPOINT}
        region: auto
        access-key-id: ${R2_ACCESS_KEY_ID}
        secret-access-key: ${R2_SECRET_ACCESS_KEY}
        force-path-style: true
